## Zed

Copy or symlink `zed/hearthstone.json` into your Zed themes folder:

```sh
ln -s "$(pwd)/zed/hearthstone.json" ~/.config/zed/themes/hearthstone.json
```

Activate via `Cmd+K Cmd+T` and pick Hearthstone.